# MONGODB Persistence Guide

This generated service uses a domain repository port so controllers and services stay independent of
database technology. Add new databases by creating another adapter that implements the same domain
repository interface.

The selected adapter and configuration are generated only for `MONGODB`.
